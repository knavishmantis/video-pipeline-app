terraform {
  required_version = ">= 1.0"

  required_providers {
    google = {
      source  = "hashicorp/google"
      version = "~> 5.0"
    }
    random = {
      source  = "hashicorp/random"
      version = "~> 3.0"
    }
  }

  # Remote state backend configuration
  # This is configured in backend.tf to allow for variable bucket names
  # The backend block in backend.tf will be used instead of this if present
}

